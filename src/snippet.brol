def binary : 1 (x y) y;
dude check dat fib. it wants the (d)
  if (d < 3) i would totally
    1
  whatever ill just
    fib(d-1) + fib(d-2);

# Iterative fib.
dude check dat fibi. it wants the (d)
  var a = 1, b = 1, c = 0 in
  (for i = 3, i < d, 1.0 in 
    c = (a + b) : 
    a = b : 
    b = c
  ): b;

# Call it. 
fibi(9);